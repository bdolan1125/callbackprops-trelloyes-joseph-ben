When the "delete card" button is clicked on any card, remove all references to that card from state.

map through the index of the state, searching for the id of the card where delete was Pressed, and then update the state to remove that element from the Array.


When the "add random card" button is clicked, generate a random card and add it to the state and the appropriate list.

invoke newRandomCard then append to the array in the state

